package com.wisso.wizefiles.core.entitlement

/**
 * Legacy capability identifiers retained while former paywall call sites are gradually simplified.
 *
 * WizeFiles 1.0.0 is GPL-3.0 software and all implemented features are available without a
 * purchase, subscription, license token, or entitlement check.
 */
enum class ProFeature {
    DUAL_PANE,
    CROSS_PANE_DRAG_AND_DROP,
    SAVED_TRANSFER_WORKFLOWS,
    UNLIMITED_REMOTE_CONNECTIONS,
    RCLONE_POWER_USER,
    SYNC_PROFILES,
    SCHEDULED_SYNC,
    ADVANCED_ARCHIVE_OPERATIONS,
    BATCH_APP_MANAGER_OPERATIONS,
    PACKAGE_SIGNING,
    MULTIPLE_VAULTS,
    VAULT_AUTOMATION,
    ROOT_TOOLS,
    BUILT_IN_SERVERS,
    ADVANCED_LOCAL_SHARE,
    SAVED_CLEANUP_RULES,
    AUTOMATIC_CLEANUP,
    PREMIUM_APPEARANCE,
}
