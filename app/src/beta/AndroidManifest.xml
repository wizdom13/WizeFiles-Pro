<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="com.wisso.wizefiles.beta.BetaExpiryProvider"
            android:authorities="${applicationId}.beta_expiry_provider"
            android:exported="false"
            android:initOrder="100" />

        <activity
            android:name="com.wisso.wizefiles.beta.BetaExpiredActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:theme="@style/Theme.WizeFiles" />
    </application>
</manifest>
