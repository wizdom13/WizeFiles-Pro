# Copy this file to signing.properties for local release signing.
# CI may provide the equivalent STORE_FILE, STORE_PASSWORD, KEY_ALIAS,
# and KEY_PASSWORD environment variables instead.
storeFile=/absolute/path/to/wizefiles-release.jks
storePassword=
keyAlias=
keyPassword=
